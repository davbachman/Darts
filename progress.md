Original prompt: Build a greenfield web game using MediaPipe to simulate throwing toward a target. Include variants for darts, wizard spells, basketball, and slingshot, with only darts playable now. Darts should show a distant target, spawn a foreground dart while the user pinches index finger and thumb, aim with hand x/y, launch by pushing toward the screen and un-pinching, estimate launch velocity from the smoothed final 150-250 ms hand trajectory, smooth jitter, and animate a subtle curved dart flight.

Progress:
- Scaffolded a Vite TypeScript app.
- Installed Three.js, MediaPipe Tasks Vision, Vitest, jsdom, and Playwright.
- Beginning tests-first implementation for input, scoring, rounds, and variants.
- Added tested core modules for gesture smoothing/release velocity, simplified ring scoring, three-dart rounds, and variant metadata.
- Copied local MediaPipe wasm files and downloaded the official Hand Landmarker model bundle into `public/mediapipe`.
- Added the app shell, Three.js darts scene, MediaPipe provider, local synthetic test provider, and browser state hooks.
- Browser screenshot showed the 3D board and dart; state inspection found a negative deterministic-progress timestamp issue, now being corrected.
- Fixed deterministic frame delta clamping and added renderer buffer preservation for canvas pixel checks.
- Verified menu screenshot, darts gameplay screenshot, three-dart synthetic round state, desktop/mobile nonblank WebGL pixel reads, and no browser console errors in the Playwright checks.
- Follow-up changes: added locked Axe Throw variant, lowered the default release hysteresis so a normal unpinch launches, replaced the blocky dart mesh with a more detailed dart, and changed ready-dart orientation so it points downrange toward the board.
- Browser verification after follow-up: menu shows all five variants in the opening viewport, ready dart is no longer upright, and synthetic throws complete a three-dart round using the lower unpinch ratio.
- Second tuning pass: shortened dart flight duration, changed the cubic flight to a concave-down arc, added a darker launch-to-current in-flight trail, and rebuilt the tail as four radial fins rooted on the rear shaft instead of one-sided/shared-plane fins.
- Third tuning pass: replaced the remaining cubic flight with an explicit parabolic arc term, increased arc height so the path reads clearly in camera projection, rendered the trail as a blue tube, and lowered release threshold from 0.47 to 0.43 for more sensitive unpinch detection.
- Fourth tuning pass: removed the trajectory preview/trail entirely and fixed in-flight orientation by correcting Three.js `lookAt` for the dart model's local nose axis (`-Z`), so the dart nose points toward the target during flight.
- Fifth tuning pass: kept in-flight darts level by yawing toward the board in the horizontal plane instead of pitching up or down along the arc.
- Variant implementation pass: added red/green tests for playable variant metadata, straight/low/tall projectile paths, and gesture profiles for wizard fist flick, axe fist-to-point, basketball palm turn, and slingshot pullback release.
- Implemented playable wizard spells, axe throw, basketball, and slingshot variants through shared variant config. Browser captures verified playable menu cards, wizard fireball/scorch, axe sticking, slingshot dent state, basketball open-center target/made outcome, and a darts regression throw.
- Slingshot visual pass: replaced the tripod-like model with a Y-shaped wooden frame, thicker elastic bands, and a pouch/marble that moves backward and downward as hand depth pulls away. Added `computeSlingshotPullback` tests and verified relaxed, full-pull, and release screenshots.
- Slingshot range tuning: reduced pouch/marble travel and scale amplification so normal hand pullback keeps the marble onscreen. Added a compact-travel regression test and verified synthetic pullback state around 0.87 still renders onscreen.
- Slingshot depth calibration: changed pullback from fixed absolute depth thresholds to a relative baseline captured when the slingshot is first pinched. Added tests for close-screen pinch baseline, visible small pullback, compact max travel, and launch still verified through synthetic browser capture.
- Slingshot live-hand calibration: added apparent hand-size depth so near-to-far motion responds before landmark z shifts, let the relaxed baseline follow the closest held pinch position, lowered the minimum pull needed for release, and verified release velocity now scales with pull distance.
- Slingshot release regression fix: release now uses the stored held pull range instead of the release frame's depth, so opening the pinch cannot erase the pullback. Added regression tests for release-frame depth jumps and thumb/index bounds expansion, then verified unit tests, build, and synthetic slingshot active flight.
- Slingshot pinch lifecycle fix: marble/slingshot state now clears when the hand is no longer pinched without launching, relaxed depth is captured once at pinch entry instead of drifting, and slingshot velocity is measured from that initial pinch baseline. Added red/green tests for baseline capture/reset, non-launch unpinch hiding, and later closer-hand positions not changing launch strength.
- Slingshot marble visibility fix: slingshot frame now stays visible while the pouch marble is hidden until pinch, and the pouch marble is offset to the target side of the pouch. Added tests for persistent frame visibility, pinch-only marble visibility, and target-side marble offset.
- Slingshot marble clearance fix: moved the pouch marble farther to the target side so its rear clears the pouch instead of intersecting. Tightened the target-side offset test, verified full tests/build, and checked a held slingshot browser capture.
- Slingshot lower-screen false-pinch fix: capped the hand scale used for gesture normalization so large/offscreen lower-hand bounds cannot classify an open thumb-index pair as pinched. Added regression tests for lower-screen open-hand landmark mapping and for slingshot not arming/releasing from that open-hand movement.

- Live webcam QA fix pass (user report: basketball not releasing, axe never showing, wand not firing, wand/axe should move in the plane perpendicular to the target): root cause for wizard/axe was `estimateHandShape` checking pinch before fist, so a real fist with a tucked or away-pointed thumb projects a tiny thumb-index distance and classifies as `pinched`, never `fist` — arming was impossible live (and the gesture-gated axe therefore never appeared). Fixes: wizard/axe hold now accepts closed hands (`fist` or `pinched` shape); axe ready object is always visible like the other non-dart variants; wizard release drops the fist requirement and fires on raw thumb toward-screen OR smoothed `wandTargetTilt >= 0.3` with arming hysteresis (`tilt <= 0.15`) plus a 2.2 minimum release velocity; basketball release adds a forward-velocity fallback (`>= 1.7` depth velocity) so a hand push releases even when per-landmark finger direction stays `across`. Fixed a double-fire bug found via synthetic trace: hold entry now reseeds the smoothing history from the raw frame so a stale high smoothed tilt from the previous cast cannot instantly re-release.
- Perpendicular-plane motion: `readyPosition` takes hand depth for wand/axe and moves them downrange (up to ~1.56 units) as the hand pushes toward the screen; fireball launches from the pushed-forward wand tip. Held wand orientation reworked from the end-on away-from-target tip offset (which hid the shaft) to a vertical-plane sweep: tip up-and-back while armed, blending to downrange as the thumb tilts.
- Asset pass: wand rebuilt (tapered walnut shaft, leather grip with gold rings, gold collar and pommel, elongated emissive octahedron crystal with point light); axe rebuilt as a bearded throwing axe (tapered wrapped handle, butt knob, dark steel socket/poll, extruded two-tone head with bright ground cutting edge). Axe flight now tumbles end-over-end around the horizontal axis (quaternion `Qx(tumble)*Qy(yaw)` with yaw biased ~0.35 rad off downrange so the blade face stays readable) and lands with a deliberate stuck pose (blade in, handle up-and-out, yaw bias 0.55).
- Verification: 75 Vitest tests pass, production build passes, synthetic browser runs confirm wizard full round scoring 110 across the three aim points (no spurious releases), axe stick + re-arm, basketball swish, clean console. Screenshot parking trick for this codebase: `window.requestAnimationFrame = () => 0` then drive `advanceTime` (remember rounds freeze gesture state after 3 throws — click `#overlay-replay` first); reload the page afterward to restore the loop.

- Basketball aim anchor: user feedback that basketball tracking thumb+index felt unnatural for an open palm; added per-variant `aimAnchor` ('pinch' | 'ring-finger') to `GameVariantConfig`, threaded through `createDefaultHandProvider` -> `MediaPipeHandInputProvider` -> `createHandFrameFromLandmarks`. A ring-finger anchor derives aim x/y and finger depth from the ring fingertip (landmark 16, pinch-midpoint fallback if missing) while pinch ratio/hand shape stay thumb+index based; basketball uses ring-finger, all other variants keep pinch. The webcam preview now highlights the ring fingertip for ring-anchored variants instead of thumb/index. Red/green landmark tests cover both anchors; 77 Vitest tests and the production build pass, synthetic basketball round regression-verified with a clean console (synthetic input bypasses landmarks, so the anchor itself needs live webcam QA).

- Fist-based control for wizard/axe (user feedback: these modes should be controlled by fist position, not thumb-index pinch): added a `'fist'` aim anchor that derives aim x/y and finger depth from the palm center (average of wrist + four finger MCPs, landmarks 0/5/9/13/17) — wizard-spells and axe-throw now use it, and the webcam preview highlights those five landmarks in fist modes. Rewrote `estimateHandShape` to detect fists with palm-relative landmarks instead of pinch-first ordering: a hand is a fist when all four fingertips curl near the palm center AND the thumb-index midpoint sits near the palm (< 0.34 of hand scale); a genuine pinch keeps the thumb-index midpoint out on the extended index finger and still classifies as `pinched` even with casually curled middle/ring/pinky (regression-guard test added so darts/slingshot are unaffected — their trackers use pinch ratio only, not shape). With reliable fist classification, wizard/axe arming requires a true `fist` again and the closed-hand (`pinched`-accepted) workaround was removed; a genuine pinch no longer arms the axe. 80 Vitest tests and the production build pass; synthetic wizard and axe rounds both complete at score 110 with a clean console. The palm-center anchor path needs live webcam QA like the other anchors.

- Axe release transition fix (user report: axe fired while the hand was still a fist): the release predicate still contained the old compact-projection workaround (`shape !== 'pinched' && toward-screen`), and a knuckles-forward fist's curled index tip can cross the toward-screen z threshold, so held fists self-fired. Axe release now requires a true fist -> open transition: hold arms on any `fist` (the direction guard was dropped since a fist can never satisfy the open-shape release), release fires only on `shape === 'open' && fingerDirection === 'toward-screen'`. To keep the original motivating case working (an open hand pointing at the screen foreshortens onto the palm), `estimateHandShape` now measures fingertip curl and the thumb-index gap in 3D — landmark depth separates a pointing open hand (tips far in z -> `open`) from a real fist (tips near the palm in all axes); the fist midpoint check stays 2D. Added landmark regression tests for the foreshortened pointing hand plus tracker tests that a fist reading toward-screen arms without firing and never releases while still a fist. 81 Vitest tests and the production build pass; synthetic axe and darts rounds both complete at 110 with a clean console.

- Held axe orientation fix (user report: blade pointed sideways while held but toward the target when thrown): the held pose still used a leftover euler rotation that left the model's +x blade axis pointing screen-right. The held axe now shares the flight orientation convention through `axeOrientationQuaternion`: blade yawed downrange with the same camera-visibility bias as flight (`axeFlightYaw` = pi/2 - 0.35), head cocked back toward the thrower (`heldAxeCockTumble` = 0.4), nudged slightly by aim. Added `heldAxeBladeDirection` export with red/green tests asserting the held blade points downrange (z < -0.7 centered, z < -0.6 across the aim range) instead of sideways. 83 Vitest tests and the production build pass; synthetic browser capture confirms the held axe reads blade-toward-board, and the console is clean.

- Removed the aim reticle and webcam preview at user request: deleted the board-plane targeting circle (`createAimReticle`/`updateAimReticle`/`setReticleColor`, plus the `aimReticleVisible` text-state field) and the corner camera preview canvas (`drawHandPreview`, `previewAnchorLandmarks`, `.hand-preview` CSS) along with the provider-side `getPreview()`/`HandPreview` API and `lastLandmarks` tracking that existed only to feed it. The `aimAnchor` config still drives aim mapping; only its preview visualization is gone. 83 Vitest tests and the production build pass; browser check confirms no reticle on the board, no preview canvas in the DOM, no `aimReticleVisible` in `render_game_to_text()`, and a clean console.

TODOs:
- Manual webcam QA is still needed on a real desktop browser because automated tests use the hidden synthetic landmark provider.
- Basketball miss/bounce has pure test coverage via `resolveBasketballImpact`; automated visual capture currently verifies made/pass behavior because the synthetic browser scenario lands center shots in the sampled state.

Update:
- Investigating report that Basketball does not show a ball. Current synthetic capture shows the basketball mesh during held/flight states; no-camera capture reproduces hoop-only view because the ready object is hidden outside the held gesture. Added a red unit test for basketball ready-ball visibility before changing renderer logic.
- Fixed basketball ready visibility: basketball now has a visible foreground ready ball before the exact shot-hold gesture is recognized, while darts/wizard/axe remain gesture-gated and the slingshot frame remains visible. Added unit coverage, verified idle/no-camera and synthetic basketball screenshots, confirmed a synthetic made shot, ran the full Vitest suite, and built production.
- Basketball gesture follow-up: user clarified release should be driven by fingers moving from away from the camera to toward the camera. Added a red gesture test without palm-orientation data, confirmed the old code stayed idle, then changed basketball hold/release predicates to open-hand away/toward finger direction while keeping the tall-arc flight config.
- Verification for basketball away/toward gesture: full Vitest suite passed with 48 tests, production build passed after removing the now-unused palm-orientation resolver, and Playwright screenshot `/tmp/handthrow-basketball-away-toward-arc/shot-0.png` showed the basketball high above the hoop during outbound flight.
- Basketball release bug follow-up: release can fail live because a hand pointing toward/away from the camera may project compactly and be classified as `fist`, so the old open-hand requirement prevented arming/release. Added a red regression for compact projected basketball hands, then changed basketball to use non-pinched hand plus finger direction instead of requiring `open`.
- Verification for compact-projection basketball release: targeted gesture tests passed with 23 tests, full Vitest suite passed with 49 tests, production build passed with the existing Vite chunk-size warning, and Playwright capture `/tmp/handthrow-basketball-release-compact-fix/shot-0.png` showed a released outbound basketball high over the hoop.
- Wizard spells update in progress: added `thumbDirection` from thumb MCP/tip depth, introduced `thumb-wand-flick` so wizard holds on fist + thumb-away and releases on fist + thumb-toward, updated synthetic wizard input, pointed the held wand away from the target, and moved fireball launch start to the target-facing wand tip. Targeted gesture/game suites passed with 32 tests.
- Wizard spells verification: full Vitest suite passed with 54 tests, production build passed with the existing Vite chunk-size warning, held screenshot `/tmp/handthrow-wizard-thumb-spells/shot-0.png` showed the foreshortened away-pointed wand, and release screenshot `/tmp/handthrow-wizard-thumb-spells-flight/shot-0.png` showed `gestureState: released` with active outbound `fireball`.
- Wizard wand visibility fix: user reported the wand was not visible. Reproduced that the held wand was effectively end-on to the camera because the away-from-target tip offset had zero screen-space x/y. Added a regression requiring lateral held-tip separation, changed the held wand to a diagonal away-from-target vector, aligned the wand mesh to that vector, and verified `/tmp/handthrow-wizard-wand-visible-fix/shot-0.png` shows the shaft clearly.
- Wizard wand always-visible follow-up: the wand was still hidden unless the exact thumb-away fist hold was recognized, making live camera setup look wandless. Added a regression for pre-hold fireball ready visibility, changed fireball ready display to stay visible like basketball/slingshot frame, and verified `/tmp/handthrow-wizard-wand-visible-idle/shot-0.png` shows a clear diagonal wand with glowing tip while gesture release remains thumb-away to thumb-toward.
- Wizard wand hand-rotation follow-up: the wand was visible but its orientation was fixed because hand rotation was not part of `HandFrame` or `ThrowInputSnapshot`. Added `wandAngle` from the thumb MCP-to-tip screen vector, smoothed it through the gesture tracker, exposed it in text state, and used it to rotate the held fireball wand while keeping the away-from-target z offset. Targeted tests passed, build passed, and held synthetic captures `/tmp/handthrow-wizard-angle-zero/shot-0.png` and `/tmp/handthrow-wizard-angle-twelve/shot-0.png` show different visible wand orientations with different `wandAngle` values.
- Wizard wand target-tilt correction: user clarified the wand should rotate toward the target, not spin in the screen plane. Replaced the `wandAngle` screen-plane path with `wandTargetTilt` derived from thumb depth delta, smoothed it through the gesture tracker, and changed held wand rotation so x/y stays readable while z tilts from away-from-target to downrange. Browser captures `/tmp/handthrow-wizard-final-tilt-away/shot-0.png` and `/tmp/handthrow-wizard-final-tilt-toward/shot-0.png` show held wand states on opposite sides of the target-depth tilt with no active flight.
- Menu simplification: user paused work on Axe Throw and Wizard Spells. Kept both variants implemented and playable in the registry/config for future fixes, but added a menu-visible variant selector so the opening menu now shows only Darts, Basketball, and Slingshot. Browser capture `/tmp/handthrow-menu-hidden-variants/shot-0.png` and state file confirm the hidden variants are absent from the menu.
- Unused-variant fix pass: re-enabled Wizard Spells and Axe Throw in the menu after fixing their live gestures. Axe release had the same compact-projection failure previously fixed for basketball (a hand pointing at the screen reads as `fist`, so the old `open`-hand release never fired); it now releases on any non-pinched hand pointing toward the screen, and arming requires the fist not already point at the screen. Wizard arming was relaxed from thumb exactly away-from-screen to any fist whose thumb is not toward the screen, keeping the deliberate thumb-toward flick release. Added red/green tracker tests for both (compact-fist axe release, no-instant-arm cases, across-thumb wizard arm), updated the registry test to expect all five menu variants, and refreshed menu descriptions (including the stale basketball palm-turn copy).
- Game-feel pass: added a board-plane aim reticle that follows tracked aim and highlights while holding (`aimReticleVisible` exposed in text state), floating impact score popups (`Bullseye +50`, `Swish!`, `Rim out`, `Miss`) projected from the 3D impact point, a round-complete overlay with total and a Play again button (replaces the old HUD round text), per-variant how-to-throw instructions in the HUD from `GameVariantConfig.instructions`, synthesized WebAudio sounds (launch whoosh, stick/dent thuds, scorch, swish, bullseye ding) guarded for environments without AudioContext, and a mirrored webcam preview canvas with landmark dots in the stage corner via an optional `getPreview()` on `HandInputProvider` (hidden for the synthetic provider). Deleted dead Vite scaffold `src/counter.ts`.
- Layout fix found during verification: after a window resize the WebGL canvas's intrinsic buffer size (CSS size x devicePixelRatio) could inflate the stage height because the canvas participated in layout; the stage grid row then latched onto the inflated fixed point. Fixed by absolutely positioning `#game-canvas` inside the stage.
- Verification: full Vitest suite passed with 63 tests, production build passed with the existing chunk-size warning, and browser checks with `?testInput=synthetic` confirmed all five variants complete throws (wizard fireball scorch, axe stick, darts stick, basketball swish, slingshot dent), reticle visibility, impact popups, the round-complete overlay resetting via Play again, and a clean browser console. Note for future sessions: the preview tool's pointer clicks were unreliable on this page; drive navigation with JS clicks through eval instead, and port 5173 is often held by the neighboring HandFly project's dev server (this repo's `.claude/launch.json` uses 5174).
